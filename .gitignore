# the local symlink to the raw Screen Studio / edit folders (hundreds of MB, outside the repo)
media
.playwright-mcp/
.DS_Store
.impeccable/live/
# screenshots written by scripts/verify-deploy.cjs
.deploy-checks/
